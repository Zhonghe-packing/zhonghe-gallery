# V26 — 首次计算三段比例，之后锁定 Banner 尺寸

- Banner 首次加载时读取当时浏览器视口宽度。
- 按 18.7% / 62.6% / 18.7% 计算左、中、右三段宽度。
- 计算结果立即转换为 px，并写入 CSS 变量。
- 后续浏览器窗口 resize 不重新计算三段宽度，图片不会因为窗口变化而重新拉伸。
- 三段图片仍按固定 430px 高度、auto 宽度保持原始比例，并在各自区域内裁切。
- 顶部导航保持不吸顶。
- 手机端隐藏左右两段，仅显示中间段。
